<% layout('layout/boilerplate') %>

<div class="d-flex justify-content-center align-items-center" style="height: 70vh;">
    <div class="w-100" style="max-width: 600px;">
        <h1 class="text-center mb-5">Shorten Your Link</h1>

        <form method="POST" action="/url">
            <div class="mb-4">
                <input name="redirectURL" type="url" class="form-control form-control-lg py-3" placeholder="Paste your long URL here..." required />
            </div>
            <div class="d-grid">
                <button class="btn btn-primary btn-lg fw-bold py-2" type="submit">
                    🔗 Get Short URL
                </button>
            </div>
        </form>
    </div>
</div>
